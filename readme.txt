mesh-2.4.2是现在暂时在用的
而provisioner是2.1.2的mesh-provisioner


mesh 1dcnn是无法被provison的，会弹出gen off model bound to a key之类的，初步怀疑是provisioner的问题，但是不知道具体是什么原因

mesh是之前的那个512个点的mesh 2.1.2
mesh_1是一个中间工程，可以不用管的
mesh_2.4.2副本 是2.4.2的标准sample

